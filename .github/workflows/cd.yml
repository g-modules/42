name: CD
on:
  pull_request:
    branches: [ main ]
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test"
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build"
  publish:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build"
